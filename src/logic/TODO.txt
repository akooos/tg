1. Container classes (Tag,Property,Attribute,Set/Group,Struct,Project)
2. Create project(tags,properties,groups,structs...) objects from code
   Create test/example system "InTouch101":
    1. regist supported types, properties
    2. import from Intouch tags formats
    3. generate tags into InTouch formats
3.  Load project from taggen's own project formats
    3.1 For ease of use, first supported format should be JSON
4 Support more systems like WinCC...
5. Taggen's own language: UTDL unified tag declaration langauge

