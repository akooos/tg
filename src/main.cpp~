
#include <cstdlib>
#include <iostream>
#include "utils.h"
int main(int argc, char** argv){

  
  char s[512] = {0};
  
  std::cout << std::endl << "Begin" << std::endl;
  for ( int i = 0; i < 512 ; ++i)
     std::cout << std::hex << ( (int) s[i] ) << std::endl;
  
  std::cout << std::endl << "Finito" << std::endl;
  
    return EXIT_SUCCESS;
}

